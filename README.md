# Brain Agriculture — Backend API

API REST para gestão de produtores rurais, propriedades e culturas agrícolas, desenvolvida em **NestJS, Node.js, TypeScript e Drizzle ORM** seguindo os princípios de **Clean Architecture e DDD**.

## 🚀 Como Executar

1. **Subir o Banco de Dados:**
   ```bash
   docker compose up -d
Executar a Aplicação:
npm install
npm run start:dev

---

### 7. **Teste de Build e Inspeção do Git**
Execute o comando de build e inspecione a árvore do Git antes de salvar:

```bash
# 1. Verificar se a aplicação transpila sem erros de sintaxe ou importação
npm run build

# 2. Verificar quais arquivos estão prontos para staging
git status